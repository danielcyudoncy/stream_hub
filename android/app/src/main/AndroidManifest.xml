<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="stream_hub"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true"
        android:usesCleartextTraffic="true">
        <!-- Disable Flutter Impeller on Android. media_kit renders video through
             a Flutter external Texture; on devices whose GPU/driver fails to
             composite external textures under Impeller (e.g. Unisoc/Mali), the
             decoder produces frames but the screen stays black while audio plays
             (media-kit/media-kit#837). The Impeller opt-out is still the official
             deployment mechanism (https://docs.flutter.dev/perf/impeller). Skia
             renders external textures correctly on these devices. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:supportsPictureInPicture="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Fullscreen native ExoPlayer video Activity (NativeActivityPlayerAdapter).
             Renders through a TextureView in a plain window outside the Flutter view
             hierarchy, composited directly by Android's view system/SurfaceFlinger:
             the render path proven to display video on Unisoc/Mali devices
             (docs/PLAYBACK_ENGINEERING.md §8.3). -->
        <activity
            android:name=".NativePlayerActivity"
            android:exported="false"
            android:theme="@style/NativePlayerTheme"
            android:taskAffinity=""
            android:screenOrientation="sensor"
            android:hardwareAccelerated="true"
            android:supportsPictureInPicture="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode" />

        <!-- IJK evaluation engine Activity. Compiled only when the vendored AAR is present.
             Safe to declare here even if compiled out, as it is not exported. -->
        <activity
            android:name=".IjkPlayerActivity"
            android:exported="false"
            android:theme="@style/NativePlayerTheme"
            android:taskAffinity=""
            android:screenOrientation="sensor"
            android:hardwareAccelerated="true"
            android:supportsPictureInPicture="false"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
