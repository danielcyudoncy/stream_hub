import 'package:get/get.dart';

class AppBinding extends Bindings {
  @override
  void dependencies() {
    // Core application level dependencies go here.
    // These will be registered globally on startup.
  }
}
