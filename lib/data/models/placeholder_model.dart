// Placeholder file to satisfy folder structure and avoid empty directories.
// Actual IPTV data models (Channel, Category, Movie, Series) will be added in later phases.
class PlaceholderModel {
  final String id;
  final String title;

  const PlaceholderModel({
    required this.id,
    required this.title,
  });
}
